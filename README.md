# Quiz-master
A terminal quiz app using sockets

# About
It's a kind of normal terminal quiz app which can be played by users (devices) that are connected to the same network

# Requirements
python3
pip

# Packages
pip install fl_networking_tools


